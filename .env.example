# ChillSim Admin Panel - Environment Variables

# API Configuration (include /api/v1 in the base URL)
VITE_API_URL=https://api.chillsim.com/api/v1

# Admin API Key (REQUIRED for bulk promo code operations)
# This key must match the ADMIN_API_KEY set in the backend
# KEEP THIS SECRET - Never commit the actual .env file to version control
VITE_ADMIN_API_KEY=your-secure-admin-api-key-here

# Supabase Configuration (if needed for direct queries)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
VITE_DEFAULT_LOCALES=en,ro,fr,ar,de,hi
